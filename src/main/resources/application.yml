server:
  port: 8080
  servlet:
    context-path: /app
spring:
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  datasource:
    password: admin
    url: jdbc:postgresql://localhost:5432/rent_car
    username: postgres
  jpa:
    hibernate:
      ddl-auto: create-drop